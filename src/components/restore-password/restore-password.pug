mixin restore-password(args)
    .restore-password&attributes(attributes)
        .row.offset-modal-top
            .offset-2.offset-md-1.offset-xl-3.col-20.col-md-22.col-xl-18.tp_h-2 Восстановить пароль

            .offset-2.offset-md-1.offset-xl-5.col-20.col-md-11.col-xl-7.restore-password__form
                +flexible-form({
                    data: data.restorePassword,
                    btnText: 'Отправить',
                    passwordAgreement: true,
                    btnBack: 'Назад'
                })
